name: Generate arcade animation

on:
  schedule:
    # Eksekusi otomatis setiap 12 jam
    - cron: "0 */12 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  generate:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      # Langkah 1: Ambil kode repo
      - uses: actions/checkout@v3

      # Langkah 2: Bikin grafik kontribusi bertema Pacman arcade asli
      - name: generate pacman-contribution-graph.svg
        uses: abozanona/pacman-contribution-graph@main
        with:
          github_user_name: NikmalWahid22
          games: 'pacman'

      # Langkah 3: Simpan hanya file hasil pacman dari folder spesifiknya
      - name: push pacman-contribution-graph.svg to the output branch
        uses: crazy-max/ghaction-github-pages@v3
        with:
          target_branch: pacman-output
          build_dir: github-contribution-grid-pacman # Mengarah langsung ke folder output bawaan robotnya
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
